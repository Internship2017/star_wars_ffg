<div class="campaign-header">
  <h1><%= image_tag "http://vignette3.wikia.nocookie.net/starwars/images/5/52/Force_and_Destiny_logo.png/revision/latest?cb=20151102133120" %>
    <span class="space_between_image">Campaigns</span></h1>
</div>

<div class="row campaign-row">
  <% @campaigns.each do |campaign| %>
      <div class="col-sm-4 campaign-col">
        <div class="card">
          <div class="card-block">
            <h4 class="card-title">Character: <%= campaign[:character] %></h4>
            <h6>Campaign Name: <%= campaign[:campaign] %></h6>

            <%= link_to "Edit", edit_campaign_path(campaign[:id]), class: "btn access_button" %>
          </div>
        </div>
      </div>
  <% end %>
</div>

